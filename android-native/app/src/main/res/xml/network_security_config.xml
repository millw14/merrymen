<?xml version="1.0" encoding="utf-8"?>
<!--
  TLS ONLY, EVERYWHERE, with one deliberate hole for local development.

  This client carries a session cookie that authorises trading actions, so a
  cleartext hop is not a theoretical downgrade — it is somebody else's session.
  `cleartextTrafficPermitted="false"` is the default above; this file exists to
  allow a developer pointing the app at a laptop, and nothing else.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
