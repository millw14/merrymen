# Android / Gradle
build/
.gradle/
.idea/
*.iml
*.hprof
.cxx/
# Machine-specific: the SDK path on THIS box. Never committed — a checked-in
# local.properties is how a build breaks for everyone but its author.
local.properties
# The release key and the file that unlocks it live in ~/.merrymen-release/,
# never here (app/build.gradle.kts). These catch a copy dropped in by mistake.
*.jks
*.keystore
*.p12
keystore.properties
