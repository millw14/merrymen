# Capability matrix

Feature → the service it runs on → the work MCP added → the scope it needs →
how it is tested. "Status" is honest about what is built, verified, partial or
not supported.

| # | Feature | Existing service / data | MCP work | Scope | Tests | Status |
|---|---|---|---|---|---|---|
| 1 | Sign-in, consent, tokens | Merrymen session (wallet / Privy), identity + grant stores | OAuth 2.1 AS (PKCE S256, CIMD, DCR, RFC 8414/9728/8707/7009/9207), consent page, opaque hashed audience-bound tokens, refresh rotation + reuse revocation, personal access tokens | — | `oauth/oauth.test.ts` (20), `http.test.ts` (13), live run with Claude Code's CIMD + SDK client | Built, verified locally |
| 2 | Connected apps | — | `/connect/apps`: list, scopes, recent activity, disconnect, personal tokens | session | live run (disconnect → tokens dead) | Built, verified locally |
| 3 | Transport | — | Streamable HTTP on `/mcp`, 2026-07-28 + 2025 revisions, Host/Origin checks, 401 discovery challenge, rate limits, bounded concurrency | — | `http.test.ts`, SDK client on both protocol versions | Built, verified locally |
| 4 | Policy | — | capability per tool, scope per capability, per-call re-check, ownership re-check against the identity store, owner-bound cursors, not_found for others' objects | — | `catalog.test.ts`, cross-owner tests in every family | Built |
| 5 | Agents & controls | `agents` ledger rows, grant projection, settings (allowlisted projection) | `list_agents`, `get_agent_status`, `get_agent_controls` (limits, 24 h budget, what pause/kill/live mean and where they live) | `agents:read` | `http.test.ts`, live run | Built |
| 6 | Portfolio | equity / positions / cost_basis / class_positions / trades / flows / fee_accruals; period attribution | `get_portfolio`, `get_trades`, `get_trade`, `get_performance`, `compare_paper_live`, `get_exposure`, resource `agents/{agent}/portfolio` | `portfolio:read` | `portfolio.test.ts` (20) | Built. Limits: paper holdings only while paper is the current book (positions has no mode); Trencher-vault custody not distinguishable (column not mirrored); per-trade fee not mirrored |
| 7 | Decisions & inactivity | decisions / trades.reject_rule / events / agents / equity / mirror_state; live-blocker and reject-rule vocabularies | `list_decisions`, `get_decision`, `get_refusals`, `explain_agent_inactivity`, resource `agents/{agent}/decisions/{id}` | `decisions:read` | `decisions.test.ts` (29) | Built. Pause state is inferred from events (the flag lives on the agent's machine) |
| 8 | Market | GeckoTerminal/CoinGecko reader, candles, pool evidence, discovery screen, stock registry, token guards | `search_tokens`, `get_token`, `get_candles`, `get_pool_activity`, `discover_tokens`, `check_token_eligibility`, watchlist | `market:read`, `agents:read`, `watchlist:manage` | `market.test.ts` (34) | Built. Owner's own price floors not visible to web (defaults used); trencher route not evaluated |
| 9 | Chat & research | partner-runtime state + partner chat surface (cannot execute) | `send_message` (pending→complete, idempotent, proposals stripped), `get_conversation`, `list_conversations`, `submit_research`, `list_research` | `chat:write` | `chat.test.ts` (23) incl. no-mutation proof | Built. Research notes reach conversations, not the trading loop |
| 10 | Reports | ledger | `get_summary`, `create_export` (CSV/JSON, 24 h, formula-safe), `get_export`, `list_exports`, download route, resource `exports/{id}` | `reports:read` | `reports.test.ts` (29) | Built |
| 11 | Public & social read | leaderboard, theses, profiles | `list_public_agents` (no dollar curve), `get_public_agent`, `get_public_theses`, `explain_leaderboard` | `market:read` | `public.test.ts` (23) | Built |
| 12 | Quotes & trade proposals | Uniswap quoter (same routes/gates as the worker), owner-order queue, worker gates, on-chain wall | `quote_trade`, `propose_trade`, `get_proposal`, `list_proposals`, `cancel_proposal`, approval page + API | `trade:propose` | `proposals.test.ts` (18), live run (real quote, approve → queued → cancel) | Built. Launchpad-curve and hooked-v4 tokens can't be quoted from web; tokens outside the stock list + owner-added tokens can't be proposed (the owner-order path addresses by symbol) |
| 13 | Setting changes & drafts | settings route (validated), chat-settable spec | `propose_settings_change` (diff), `create_agent_draft`; approval applies through the settings route as the owner | `drafts:write` | `proposals.test.ts` | Built |
| 14 | Posts | group-chat route and gate | `draft_post`; approval posts through the group-chat route as the owner | `social:write` | `proposals.test.ts` | Built |
| 15 | Follow & share | follow store, theses, evidenced sells | `follow_agent`, `unfollow_agent`, `list_following`, `share_trade_summary` | `social:write`, `agents:read`, `portfolio:read` | `social.test.ts` | Built |
| 16 | Backtests | `runBacktest`, strategies, oracle history, synthetic scenarios | durable jobs (`mcp_jobs`), orchestrator runner with leases/recovery/cancel/deadline; `run_backtest`, `get_job`, `list_jobs`, `cancel_job` | `jobs:run` | `worker/src/mcp/jobs.test.ts`, `tools/jobs.test.ts` | Built. Stock tokens only (no memecoin/curve fills); naive fills; calendar-day budget reset |
| 17 | Notifications | per-owner Telegram bot, tenant_telegram link | subscriptions + delivery ledger, orchestrator dispatcher with dedupe/retry/dead-letter; `list_notification_channels`, `subscribe`, `list_subscriptions`, `unsubscribe`, `list_deliveries` | `notifications:manage` | `worker/src/mcp/notify.test.ts`, `tools/notifications.test.ts` | Built. Telegram is the only channel; watchlist price alerts only for tokens with an oracle feed |
| 18 | Rich views | — | MCP Apps views (portfolio, decision, token, proposal) with text fallbacks | as the tool | `apps.test.ts` | Built; host rendering not verified (no Claude Desktop here) |
| 19 | Staff diagnostics | ledger (redacted) | `staff_fleet_health`, `staff_execution_failures`, `staff_provider_errors`, `staff_deployment`, `staff_mcp_metrics` | `staff:diagnostics` (allowlisted) | `staff.test.ts` (16) | Built |
| 20 | Pause / resume | Telegram-only marker on the agent's machine; blocks exits too | explained in `get_agent_controls`; **not offered** | — | — | Not supported by design (a remote pause that silently stops protective exits is unsafe; no shared state exists) |
| 21 | Kill switch, live switch, signed limits | dashboard / re-sign | explained, linked; **not offered** | — | — | Not supported by design |
| 22 | Ops | — | health, trace ids, JSON logs, metrics, audit, retention, emergency revoke | — | `maintenance.test.ts`, `background.test.ts` | Built |
